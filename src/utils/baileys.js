/**
 * Arquivo placeholder 'baileys.js'
 * Caso queira integrar com Baileys, implemente aqui:
 * - connect()
 * - on('qr', ... ) -> retornar string do QR
 * - persistência de auth state (arquivo .auth.json por sessão)
 *
 * Por enquanto este arquivo apenas exporta stubs para não quebrar importações.
 */

export async function connectWithBaileys(sessionId, opts = {}) {
  throw new Error("Not implemented. Substitua por integração com Baileys.");
}
